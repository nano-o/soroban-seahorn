# Includes one of the default files in src/bootstrap/defaults
profile = "user"
changelog-seen = 2
[llvm]
targets = "X86"
experimental-targets = ""
[install]
prefix="/home/user/rust-install/"
sysconfdir="etc"
[build]
docs = false
